# Create a list:
lNames = ["Ben", "Fred", "George"]

# Output the list on the screen:
print(lNames)

# List are given a location, these start at 0
print(lNames[0])

# This adds an item onto the end:
lNames.append("Billy")

print(lNames)

lNames.insert(1, "Frank")
print(lNames)
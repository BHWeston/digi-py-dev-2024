# This program will keep running until the phrase "stop" is typed in by the user:

# Declare the variable as having nothing inside
sPrinterText = ""

# Loop if not equal to "stop"
while sPrinterText != "stop":
    sPrinterText = input("Enter in text to display onto the screen, type stop to end the program: ")
    print(sPrinterText)
